# SECURITY.md: 마음건강 데이터 프라이버시 및 보안 명세서

MindCare Routine은 정신적 곤란을 겪는 사용자가 본인의 사적인 성찰 내역을 기록하는 만큼, 개인정보 보호 및 데이터 기밀성 수호를 제품 아키텍처 설계의 최우선 순위로 간주합니다.

---

## 1. 보안 핵심 원칙 (Core Security Principles)

1. **최소 권한 및 최소 수집:**
   서비스 가입 시 주민등록번호, 휴대폰 인증 등을 전면 배제하여 실명인증 정보를 축적하지 않습니다. 사용자는 닉네임과 이메일만으로 가입할 수 있습니다.
2. **로컬 우선 보안(Local-First Privacy):**
   비회원 상태에서 데이터를 작성하는 경우, 어떤 텍스트나 마음지표 점수도 네트워크 외부로 방출되지 않고 오직 로컬 IndexedDB/LocalStorage 내에만 암호화 및 보관 처리되어 유출 가능성을 원천적으로 차단합니다.
3. ** Row Level Security (RLS)의 강제화:**
   서버 DB(Supabase/PostgreSQL) 연동 시, 데이터 테이블은 `ENABLE ROW LEVEL SECURITY` 상태가 되며, 세션 인증 사용자 토큰이 본인의 `user_id`와 동일하지 않은 모든 쿼리 요청(`SELECT`, `INSERT`, `UPDATE`, `DELETE`)에 대해 데이터베이스 단에서 거부합니다.

---

## 2. 위기 관리 보안 정책 (Safety Escalation Policy)

* **선제적 필터링:** AI 서버 API 호출(OpenAI 등) 시 발생할 수 있는 데이터 유출 및 AI의 부적절한 심리적 개입 오류를 막기 위해, 클라이언트 및 API 게이트웨이 초입에서 2단계 안전 검증(`Pre-generation` 및 `Post-generation` 위기 분석)을 적용합니다.
* **위기 감지 시 로그 최소화:** 위기가 감지되면 이벤트 기록 시점과 감지 방식에 대한 플래그만을 남겨 긴급 조력을 유도할 뿐, 사용자가 격한 상태에서 작성한 원문 텍스트를 감사 로그에 절대 기록하지 않습니다.

---

## 3. 민감 로그 필터링 (PII Redaction)

생성되는 서버 애플리케이션 로그에 사용자의 개인정보 및 정신건강 자유 서술 텍스트가 유출되지 않도록 하는 마스킹 레이어를 제공합니다.
* **마스킹 패턴:**
  `src/lib/safety-classifier.ts`에 탑재된 `redactSensitiveLogs` 함수가 서버 로그 방출 전 다음과 같은 정보를 자동 마스킹합니다:
  * 사용자 저널 본문 (`journalContent`, `automaticThought` 등) ➡️ `[REDACTED_SENSITIVE_TEXT]`로 전환.
  * 이메일 주소 ➡️ 앞 3자리 제외 후 `***@domain.com`으로 전환.
* **광고 트래커 원천 차단:** 
  구글 애널리틱스, 페이스북 픽셀 등 제3자 광고 트래커 및 마케팅 추적 도구를 일절 배치하지 않으며 기분 지표의 분석 목적으로 애드 테크 모듈을 적재하는 행위를 금지합니다.

---

## 4. 데이터 주권과 잊힐 권리 (Right to be Forgotten)

* 사용자가 설정 페이지(`src/app/privacy/page.tsx`)에서 **'모든 데이터 즉각 완전 영구 파기'**를 실행하는 경우, Soft-delete(삭제 플래그만 변경)를 배제하고 데이터베이스 릴레이션의 해당 행(Row)을 영구 삭제(`DELETE`) 처리하여 백업 저장본 외에 실시간 디바이스 및 DB 내의 모든 잔해를 완전 소멸시킵니다.
